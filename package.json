{
  "name": "jee-mock-test-platform",
  "version": "1.0.0",
  "description": "Production-grade JEE Mock Test Platform - Netlify Ready",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
